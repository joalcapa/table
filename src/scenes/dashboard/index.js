import PropTypes from 'prop-types';

import './index.css';

const Dashboard = (props) => (
    <div>
        Dashboard
    </div>
);

Dashboard.propTypes = {
    
};

Dashboard.defaultProps = {
    
};

export default Dashboard;